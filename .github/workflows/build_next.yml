name: Community Extension Build on Latest DuckDB
on:
  pull_request:
    paths-ignore:
      - '**'
      - '!scripts/build.py'
      - '!.github/workflows/build.yml'
      - '!extensions/*/description.yml'
  push:
    paths-ignore:
      - '**'
      - '!scripts/build.py'
      - '!.github/workflows/build.yml'
      - '!extensions/*/description.yml'

jobs:
  test_against_latst:
    uses: ./.github/workflows/build.yml 
    with:
      duckdb_version: 'main'
      deploy: 'false'
